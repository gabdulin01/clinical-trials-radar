{
  "name": "clinical-trials-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive explorer for ClinicalTrials.gov data",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=20" }
}
